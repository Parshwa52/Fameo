class Company {
  constructor(email, name, tagLine, description, logo, website, type) {
    (this.email = email),
      (this.name = name),
      (this.tagLine = tagLine),
      (this.description = description),
      (this.logo = logo),
      (this.website = website),
      (this.type = type);
  }
}

export default Company;
